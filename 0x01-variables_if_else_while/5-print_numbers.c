#include <stdio.h>

/**
* main - Entry Point
*
* Return: Always 0 (Success)
*/
int main(void)
{
	int letter;

	for (letter = 48; letter <= 57; letter++)
	{
	putchar(letter);
	}
	putchar('\n');
	return (0);
}
