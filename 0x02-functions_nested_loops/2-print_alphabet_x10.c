#include <stdio.h>
#include "main.h"

/**
* main - Entry Point
*
* Return: Always 0 (Success)
*/
int main(void);
void print_alphabet_x10(void);
{
	int letter;

	for (letter = 1; letter <= 10; ++letter)
	print_alphabet();
	putchar('\n');
	return (0);
}
