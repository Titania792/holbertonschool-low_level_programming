#include "main.h"

/**
*leet - function that encodes a string into 1337.
*@a: fghj
*Return: 0
*
*/
char *leet(char *a)
{
	int b = 0;

	while (a[b] != '\0')
	{
		if (a[b] == 97 || a[b] == 65)
			a[b] == '4'
		b++;
	
}
